{
  "name": "muvera_test",
  "version": "1.0.0",
  "description": "Sistema RAG multimodal para histopatología con ColPali, ColBERT, MUVERA y Gemini.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "uv run api.py",
    "frontend": "npm run dev --prefix frontend",
    "dev": "mkdir -p histopatologia_data/embeddings histopatologia_data/cache histopatologia_data/ontologia && npm run docker:up && concurrently \"npm run backend\" \"npm run frontend\"",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "db:clear": "uv run python clear_db.py"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
